# macOS 16.0 Metadata Stub

## Summary

This folder is reserved for reduced macOS `16.0` metadata. No release-specific artifacts have been acquired yet.

## Current Status

- Runtime facts: not recorded.
- Artifact index: not created.
- Reduced datasets: not created.
- Raw artifacts: not committed.

## Before Adding Data

- Follow `docs/guidelines/release-acquisition.md`.
- Use `docs/guidelines/release-provenance-template.md`.
- Run `docs/guidelines/acquisition-checklist.md`.

## Boundaries

This stub does not claim any macOS `16.0` behavior, build number, kernel version, or driver metadata.
