# macOS 15.6 to 16.0 release diff

## Summary

- From release: `15.6`
- To release: `16.0`
- Comparison scope: release-diff input readiness for reduced metadata
- Status: unresolved

The first honest question we can answer with committed artifacts is whether the repository already contains comparable reduced metadata for a macOS `15.6` to `16.0` release diff. It does not. Both releases currently expose only derived stubs and missing acquisition states, so no behavioral, structural, or content diff can be supported yet.

## Inputs

| Input | Path | State | Notes |
| --- | --- | --- | --- |
| Artifact index | `macos/15.6/artifacts.md` | derived | States that no comparable reduced metadata has been acquired yet. |
| Artifact index | `macos/16.0/artifacts.md` | derived | States that no comparable reduced metadata has been acquired yet. |
| Acquisition manifest | `datasets/fixtures/acquisition-manifest/15.6.fixture.json` | derived + missing | Contains release-folder stub, artifact-index stub, and missing installer payload. |
| Acquisition manifest | `datasets/fixtures/acquisition-manifest/16.0.fixture.json` | derived + missing | Contains release-folder stub, artifact-index stub, and missing installer payload. |
| Reduced dataset | `datasets/fixtures/release-diff/missing-artifacts-15.6-to-16.0.fixture.json` | unresolved example | Matches current repository evidence: no comparable kext or IORegistry reduced datasets are committed. |

## Observations

- Observation: both release artifact indexes explicitly say that runtime commands, kext personality inventory, IORegistry inventory, and release diff input are not acquired.
  - Evidence: `macos/15.6/artifacts.md`, `macos/16.0/artifacts.md`
- Observation: both acquisition manifests contain only derived release-folder stubs, artifact-index stubs, and a missing installer payload record.
  - Evidence: `datasets/fixtures/acquisition-manifest/15.6.fixture.json`, `datasets/fixtures/acquisition-manifest/16.0.fixture.json`
- Observation: the existing missing-artifacts fixture already models the repository state as unresolved for kext personality and IORegistry comparison.
  - Evidence: `datasets/fixtures/release-diff/missing-artifacts-15.6-to-16.0.fixture.json`

## Inferences

- Inference: no evidence-backed release diff can yet be published for kext personalities or IORegistry topology between `15.6` and `16.0`.
  - Based on: missing reduced datasets in both release indexes and acquisition manifests
  - Confidence: high
- Inference: the correct current output is an unresolved report plus unresolved fixture references, not a claim of change or no change.
  - Based on: `docs/guidelines/release-diff-input-readiness.md` and current committed evidence paths
  - Confidence: high

## Diff Records

| Subject | Field | State | Evidence | Notes |
| --- | --- | --- | --- | --- |
| kext-personality-inventory | records | unresolved | `macos/15.6/artifacts.md`, `macos/16.0/artifacts.md`, `datasets/fixtures/acquisition-manifest/15.6.fixture.json`, `datasets/fixtures/acquisition-manifest/16.0.fixture.json` | Both releases mark this reduced dataset as not acquired. |
| ioregistry-inventory | records | unresolved | `macos/15.6/artifacts.md`, `macos/16.0/artifacts.md`, `datasets/fixtures/acquisition-manifest/15.6.fixture.json`, `datasets/fixtures/acquisition-manifest/16.0.fixture.json` | Comparable registry topology is unavailable. |
| installer-payload | availability | unresolved | `datasets/fixtures/acquisition-manifest/15.6.fixture.json`, `datasets/fixtures/acquisition-manifest/16.0.fixture.json` | Both manifests mark installer payload as missing, so no installer-derived diff can be attempted. |

## Unknowns And Unresolved Items

- Unknown: whether `15.6` and `16.0` differ in kext personalities.
  - Why it remains unresolved: no reduced kext personality datasets are committed for either release.
  - Next evidence needed: reduced kext personality inventories with committed provenance for both releases.
- Unknown: whether `15.6` and `16.0` differ in IORegistry topology or user-client exposure.
  - Why it remains unresolved: no comparable IORegistry reduced datasets are committed for either release.
  - Next evidence needed: reduced IORegistry inventory or topology captures for both releases.
- Unknown: whether installer or kernel-derived metadata differs between the releases.
  - Why it remains unresolved: both manifests mark installer payload as missing.
  - Next evidence needed: committed acquisition and reduced metadata from installer, IPSW, or extracted kernel artifacts.

## Validation

```sh
tools/schema-validate/validate-json.sh
.venv/bin/python tools/schema-validate/validate-known-schemas.py
```

Result: both commands passed on `2026-06-29`.

## Follow-Up Queue

1. Acquire or derive a reduced kext personality dataset for `15.6`.
2. Acquire or derive a reduced kext personality dataset for `16.0`.
3. Acquire comparable IORegistry reduced metadata for both releases, or keep the diff unresolved.

## References

- `docs/guidelines/release-diff-input-readiness.md`
- `datasets/fixtures/release-diff/missing-artifacts-15.6-to-16.0.fixture.json`
- `datasets/fixtures/acquisition-manifest/15.6.fixture.json`
- `datasets/fixtures/acquisition-manifest/16.0.fixture.json`
- `macos/15.6/artifacts.md`
- `macos/16.0/artifacts.md`
